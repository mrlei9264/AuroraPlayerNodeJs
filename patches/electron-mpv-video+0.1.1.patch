diff --git a/node_modules/electron-mpv-video/dist/main/mpv-service.js b/node_modules/electron-mpv-video/dist/main/mpv-service.js
index 7b27f7f..f1d3b11 100644
--- a/node_modules/electron-mpv-video/dist/main/mpv-service.js
+++ b/node_modules/electron-mpv-video/dist/main/mpv-service.js
@@ -12,6 +12,7 @@ const IPC_CHANNELS = [
     'player:stop',
     'player:seek',
     'player:set-volume',
+    'player:set-property',
     'player:set-render-size',
     'player:set-render-pipeline',
     'player:destroy',
@@ -59,6 +60,16 @@ function normalizeSource(value) {
     }
     return value;
 }
+const SETTABLE_PROPERTIES = new Set(['speed', 'mute', 'vid', 'aid', 'sid']);
+function normalizeProperty(name, value) {
+    if (typeof name !== 'string' || !SETTABLE_PROPERTIES.has(name)) {
+        throw new TypeError(`Unsupported player property: ${String(name)}`);
+    }
+    if (typeof value !== 'string' || value.length > 64) {
+        throw new TypeError('Player property value must be a short string');
+    }
+    return { name, value };
+}
 class PlayerSession {
     window;
     ownerWebContentsId;
@@ -134,6 +145,12 @@ class PlayerSession {
         this.player.setVolume(nextVolume);
         this.volume = nextVolume;
     }
+    setProperty(name, value) {
+        this.assertAlive();
+        if (typeof this.player.setProperty !== 'function')
+            return;
+        this.player.setProperty(name, value);
+    }
     setRenderSize(size) {
         this.assertAlive();
         this.renderSize = size;
@@ -259,6 +276,13 @@ class PlayerSession {
                 return;
             this.sharedTextureFrameInFlight = true;
             let imported;
+            let importedReleased = false;
+            const releaseImported = () => {
+                if (!imported || importedReleased)
+                    return;
+                importedReleased = true;
+                imported.release();
+            };
             try {
                 const textureInfo = this.player.renderSharedTexture(this.renderSize.width, this.renderSize.height);
                 const referencesReleased = new Promise((resolve) => {
@@ -274,12 +298,12 @@ class PlayerSession {
                     frame: this.window.webContents.mainFrame,
                     importedSharedTexture: imported,
                 }, this.id);
-                imported.release();
+                releaseImported();
                 await referencesReleased;
             }
             catch (error) {
                 this.sharedTextureFrameInFlight = false;
-                imported?.release();
+                releaseImported();
                 throw error;
             }
         }
@@ -463,6 +487,10 @@ class MpvMainService {
         electron.ipcMain.handle(channel('player:stop'), async (event, id) => this.getOwnedSession(event, id).stop());
         electron.ipcMain.handle(channel('player:seek'), async (event, id, seconds) => this.getOwnedSession(event, id).seek(finiteNumber(seconds, 'seconds')));
         electron.ipcMain.handle(channel('player:set-volume'), async (event, id, value) => this.getOwnedSession(event, id).setVolume(finiteNumber(value, 'volume')));
+        electron.ipcMain.handle(channel('player:set-property'), async (event, id, name, value) => {
+            const property = normalizeProperty(name, value);
+            return this.getOwnedSession(event, id).setProperty(property.name, property.value);
+        });
         electron.ipcMain.handle(channel('player:set-render-size'), async (event, id, size) => this.getOwnedSession(event, id).setRenderSize(normalizeRenderSize(size)));
         electron.ipcMain.handle(channel('player:set-render-pipeline'), async (event, id, pipeline) => this.getOwnedSession(event, id).setRenderPipeline(normalizePipeline(pipeline)));
         electron.ipcMain.handle(channel('player:destroy'), async (event, id) => this.getOwnedSession(event, id).destroy());
diff --git a/node_modules/electron-mpv-video/dist/preload/index.cjs b/node_modules/electron-mpv-video/dist/preload/index.cjs
index d1d823b..6051885 100644
--- a/node_modules/electron-mpv-video/dist/preload/index.cjs
+++ b/node_modules/electron-mpv-video/dist/preload/index.cjs
@@ -84,6 +84,7 @@ function createPlayerSession(id) {
         stop: () => electron.ipcRenderer.invoke(channel('player:stop'), id),
         seek: (seconds) => electron.ipcRenderer.invoke(channel('player:seek'), id, seconds),
         setVolume: (value) => electron.ipcRenderer.invoke(channel('player:set-volume'), id, value),
+        setProperty: (name, value) => electron.ipcRenderer.invoke(channel('player:set-property'), id, name, value),
         setRenderSize: (width, height) => electron.ipcRenderer.invoke(channel('player:set-render-size'), id, { width, height }),
         setRenderMode: (mode) => electron.ipcRenderer.invoke(channel('player:set-render-pipeline'), id, pipelineForRenderMode(mode)),
         destroy: async () => {
diff --git a/node_modules/electron-mpv-video/dist/renderer/mpv-video.d.ts b/node_modules/electron-mpv-video/dist/renderer/mpv-video.d.ts
index 6dc9221..0938d60 100644
--- a/node_modules/electron-mpv-video/dist/renderer/mpv-video.d.ts
+++ b/node_modules/electron-mpv-video/dist/renderer/mpv-video.d.ts
@@ -41,6 +41,7 @@ export declare class MpvVideoElement extends HTMLElement {
     stop(): Promise<void>;
     seek(seconds: number): Promise<void>;
     setVolume(value: number): Promise<void>;
+    setProperty(name: 'speed' | 'mute' | 'vid' | 'aid' | 'sid', value: string): Promise<void>;
     setRenderMode(mode: RenderMode): Promise<void>;
     destroy(): Promise<void>;
     private destroyInternal;
diff --git a/node_modules/electron-mpv-video/dist/renderer/mpv-video.js b/node_modules/electron-mpv-video/dist/renderer/mpv-video.js
index 355f95b..1319297 100644
--- a/node_modules/electron-mpv-video/dist/renderer/mpv-video.js
+++ b/node_modules/electron-mpv-video/dist/renderer/mpv-video.js
@@ -409,6 +409,10 @@ export class MpvVideoElement extends HTMLElement {
         }
         await this.player?.setVolume(volume);
     }
+    async setProperty(name, value) {
+        await this.ensureReady();
+        await this.player?.setProperty(name, value);
+    }
     async setRenderMode(mode) {
         await this.ensureReady();
         const nextMode = this.resolveRenderMode(mode);
diff --git a/node_modules/electron-mpv-video/dist/shared/types.d.ts b/node_modules/electron-mpv-video/dist/shared/types.d.ts
index 1da1aa4..700c474 100644
--- a/node_modules/electron-mpv-video/dist/shared/types.d.ts
+++ b/node_modules/electron-mpv-video/dist/shared/types.d.ts
@@ -27,6 +27,7 @@ export type MpvPlayerSession = {
     stop(): Promise<void>;
     seek(seconds: number): Promise<void>;
     setVolume(value: number): Promise<void>;
+    setProperty(name: 'speed' | 'mute' | 'vid' | 'aid' | 'sid', value: string): Promise<void>;
     setRenderSize(width: number, height: number): Promise<void>;
     setRenderMode(mode: RenderMode): Promise<void>;
     destroy(): Promise<void>;
diff --git a/node_modules/electron-mpv-video/native/mpv-addon/src/mpv_addon.cc b/node_modules/electron-mpv-video/native/mpv-addon/src/mpv_addon.cc
index 46deaa6..90a5d58 100644
--- a/node_modules/electron-mpv-video/native/mpv-addon/src/mpv_addon.cc
+++ b/node_modules/electron-mpv-video/native/mpv-addon/src/mpv_addon.cc
@@ -132,6 +132,7 @@ class MpvPlayer : public Napi::ObjectWrap<MpvPlayer> {
       InstanceMethod("stop", &MpvPlayer::Stop),
       InstanceMethod("seek", &MpvPlayer::Seek),
       InstanceMethod("setVolume", &MpvPlayer::SetVolume),
+      InstanceMethod("setProperty", &MpvPlayer::SetProperty),
       InstanceMethod("setUpdateCallback", &MpvPlayer::SetUpdateCallback),
       InstanceMethod("setEventCallback", &MpvPlayer::SetEventCallback),
       InstanceMethod("renderFrame", &MpvPlayer::RenderFrame),
@@ -165,12 +166,16 @@ class MpvPlayer : public Napi::ObjectWrap<MpvPlayer> {
     set_option("audio-display", "no");
     set_option("pause", "yes");
     set_option("keep-open", "yes");
+    set_option("cache", "yes");
+    set_option("demuxer-readahead-secs", "60");
+    set_option("demuxer-max-bytes", "268435456");
+    set_option("demuxer-max-back-bytes", "67108864");
+    set_option("stream-buffer-size", "4194304");
+    set_option("cache-pause", "yes");
+    set_option("cache-pause-wait", "2");
+    set_option("network-timeout", "30");
     const char* hwdec = std::getenv("MPV_HWDEC");
-#ifdef _WIN32
-    set_option("hwdec", hwdec && hwdec[0] ? hwdec : "no");
-#else
     set_option("hwdec", hwdec && hwdec[0] ? hwdec : "auto-safe");
-#endif
     set_option("sw-fast", "yes");
     set_option("vo", "libmpv");
     set_mpv_wakeup_callback(handle_, on_mpv_wakeup, this);
@@ -189,6 +194,15 @@ class MpvPlayer : public Napi::ObjectWrap<MpvPlayer> {
     observe("height", MPV_FORMAT_INT64);
     observe("video-codec", MPV_FORMAT_STRING);
     observe("container-fps", MPV_FORMAT_DOUBLE);
+    observe("demuxer-cache-duration", MPV_FORMAT_DOUBLE);
+    observe("demuxer-cache-time", MPV_FORMAT_DOUBLE);
+    observe("cache-buffering-state", MPV_FORMAT_INT64);
+    observe("paused-for-cache", MPV_FORMAT_FLAG);
+    observe("speed", MPV_FORMAT_DOUBLE);
+    observe("mute", MPV_FORMAT_FLAG);
+    observe("vid", MPV_FORMAT_STRING);
+    observe("aid", MPV_FORMAT_STRING);
+    observe("sid", MPV_FORMAT_STRING);
 
     if (mode_ == "shared-texture") {
 #if defined(__APPLE__)
@@ -359,6 +373,19 @@ class MpvPlayer : public Napi::ObjectWrap<MpvPlayer> {
     return env.Undefined();
   }
 
+  Napi::Value SetProperty(const Napi::CallbackInfo& info) {
+    Napi::Env env = info.Env();
+    if (info.Length() < 2 || !info[0].IsString() || !info[1].IsString()) {
+      Napi::TypeError::New(env, "setProperty(name, value) requires two strings").ThrowAsJavaScriptException();
+      return env.Undefined();
+    }
+    std::string name = info[0].As<Napi::String>().Utf8Value();
+    std::string value = info[1].As<Napi::String>().Utf8Value();
+    int ret = mpv_set_property_string(handle_, name.c_str(), value.c_str());
+    if (ret < 0) throw_mpv_error(env, "setProperty", ret);
+    return env.Undefined();
+  }
+
   Napi::Value SetUpdateCallback(const Napi::CallbackInfo& info) {
     Napi::Env env = info.Env();
     if (update_callback_) {
